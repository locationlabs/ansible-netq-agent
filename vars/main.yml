---

netq_backend_config:
  netq-agent:
    port: 31980
    server: "{{ netq_backend_server|mandatory }}"
    vrf: default
  netq-cli:
    netq-user: admin
    port: 32708
    server: "{{ netq_backend_server|mandatory }}"

netq_repo: "{{ netq_repo_version }}"
